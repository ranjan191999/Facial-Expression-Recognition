# Emotion→Review RAG

Complete repo: facial emotion recognition → synthetic review generation → embeddings → FAISS → LangChain RAG → FastAPI + Streamlit demo.

## Setup

```bash
python -m venv .venv
source .venv/bin/activate   # Windows: .venv\Scripts\activate
pip install -r requirements.txt
